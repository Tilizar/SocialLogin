apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'

android {
    compileSdkVersion 28

    defaultConfig {
        minSdkVersion 19
        targetSdkVersion 28
    }
    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
    implementation project(':social-login-core')
    implementation 'org.jetbrains.kotlin:kotlin-stdlib-jdk7:1.3.0'
}

ext {
    bintrayName = 'instagram-explicit'
    artifact = 'instagram-explicit'
    libraryDescription = 'Social Login - instagram-explicit'
}

apply from: '../publish.gradle'